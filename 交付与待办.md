# 《字启千年》本轮交付与待办

更新时间：2026-09-18。以下是当前代码的实际状态，不等于正式比赛验收或古文字学术审核已经完成。

## 最新四项参赛功能（2026-09-18）

四项功能全部离线、零后端，`node verify/run-all.mjs` **36 项通过**（23 Node + 8 CDP + 5 页内自检）：

1. **双语开关**（顶部导航 中/EN）：外壳文案（导航/按钮/页签/状态栏/帮助/空态）走字典 `src/ui/i18n.js`（zh/en 186 键一致，缺键回退中文）；内容级英文只做 tier=core 36 字一行释义 `data/en.js`（`ZQ.CharEN`，显示时缺字回退中文释义），拼音本就是拉丁字母；长文化文案保持中文不抢跑。英文释义为 AI 起草，**提交/演示前请人工校对用词**。
2. **作品墙**：「我的造字-字库」卡片升级为 120×168 海报缩略图（`drawComposedGlyph` 离屏绘制），每件可「重新导出 PNG」（复用海报渲染 `renderPoster`）。
3. **作品分享链接**：卡片「分享」→ `#share=MINE1编码`（单件作品编码，纯文本可 diff）；打开链接 → 预览弹窗 → 「保存到我的字库」才写库，坏码只提示。
4. **主题字包**（首页新区块）：只给已收录 134 字打主题标签（`data/themes.js`，不改 catalog 之外的真源）：自然/人体/数字/农耕/动作/生肖。**生肖同日凑齐 12 字**（补 兔蛇猴鸡狗猪，tier=extended，Catalog 128→134；古文字缺期由管线如实标记，不补造）。

回归：`test-delivery.mjs` 扩充（#share 一次性消费、分享码往返、双语基础），新增 `test-themes.mjs` 与 `test-i18n.mjs`。浏览器视觉与拖拽仍未验收；英文释义与主题归类待人工校对。

## 刷新闪字形（2026-09-18）

**用户报的**：「刷新网页后会弹出来一秒别的样式的网站界面」。

**病根**：站上三个字体是 **45.8 MB**（NotoSansSC 17.7 + NotoSerifSC 25.1 + LongCang 5.2），
而 `@font-face` 写着 `font-display:swap`。swap 的机制是**先用后备字形把整页画一遍**
（Windows 上落到雅黑 / 宋体），字体下完再**整体换掉**。冷启动逐帧实测：第 54 ms 登记
`@font-face`、第 370 ms（最坏 1011 ms）才加载完；第 297 ms 那一帧的截图里，页眉是细宋、
正文一片空白 —— 与用户描述完全一致。

**这个缺陷修过两次，第一次没修掉，因为两次量的东西都不对**，这条教训比修复本身重要：

1. 第一版只藏了页脚。当时的论证是「页眉挂载前后像素一致（top 28 / 高 100 / 字号 49.464px
   都不动）」—— **量的是盒子几何，而换字形根本不改盒子**，它改的是字形本身。
2. 第二版才找对方向：问题不在「哪个元素露了」，而在「整页用错的字形画过一次」。

**做法**（两步，缺一不可）：

- **子集化**（治根）：三个字体 45.8 MB → **3.91 MB**（12×），字符集 = 站点用字（1515 个）
  ∪ GB2312 一级常用字（3755 个，因为「05 我的造字」里用户手打的是字表外任意文字）。
  由 `verify/build-font-subset.py` 生成，**原件一个字节都没动**，仍在 `assets/fonts/`。
  打包时排除哪些、代价是什么，见新增的 [assets/fonts/README.md](assets/fonts/README.md)。
- **放行闸门**（兜底）：`ZQ.revealWhenReady` 让整个 `.zc-app` 等到**挂载完成 + 字体到位**
  才显示。用 `visibility:hidden` 而不是 `display:none` —— 后者会让浏览器不为这些文字做布局，
  也就不去请求字体，闸门会绕成死锁。超时 2 s 兜底，拿不到字体也要放行（宁可看到后备字形，
  也不能永远白屏），且写在 `try/finally` 里（挂载失败也要放）。

**验证**：

- `verify/test-fonts.mjs`（新增，纯 Node）：**自己解 woff2 的 cmap**（WOFF2 只对 glyf/loca
  做变换，cmap 是原样进 brotli 流的，`node:zlib` 自带的 `brotliDecompressSync` 就够，
  不引入依赖）—— 证明的是**浏览器实际拿到的那个文件**，比比对 `charset.txt` 清单强得多
  （清单只能证明清单对，证不了字体对）。判据是「**丢没丢**」而不是「有没有」：字符集是扫
  源码正文扫出来的，注释里的 ✅ 和变体选择符 U+FE0F 也在内，源字体本来就没有它们，
  子集化前后一样落到系统字体，不算子集的错。
- `verify/test-boot.mjs`（新增，CDP）：**必须用全新 profile**（`mkdtempSync`）。字体进过缓存
  就没有那个窗口了，用例会永远绿 —— 这正是它上一轮漏掉缺陷的原因。逐帧录下启动过程，
  只判定一件事：**`.zc-app` 第一次可见的那一帧，三个字体是否都已 loaded**。
  实测 **1049 ms → 430 ms**，字体在第 295 ms 全部到位。
- **像素比对**（一次性，脚本不进仓库）：与改之前的 7 张基准图逐张比。结论分三层 ——
  ① `verify/test-fonts.mjs` 之外单独做了 canvas 隔离测：**340/340 逐像素一致**
  （3 字体 × 5 个字重 300–900 × 68 个样例字，含生僻字），前进宽度全部相同；
  ② 06 整页 **逐字节相同**；02 / 03 / 05 用「注入原件 `@font-face` 覆盖」跑出来
  **与子集逐字节相同**；③ 残余差异 ≤3/255（99.96% 的差异像素恰好差 1），而且**两次都用
  原件跑出来的也会差这个量**（50% 有差、峰 3/4）—— 所以那是运行间的抖动，不跟着字体走。
  布局尺寸逐位相同（`docH 1786`、`appH 1785.84375`、`hdrH 100.359375`，两种字体完全一致）。

**仍然没做的**：在**你本机**的真实浏览器里刷新一次看效果（自动化只能证明「可见那一帧字形
已对」，证明不了「你觉得快了」）。以及提交包要不要瘦身 —— 那是取舍，见
[assets/fonts/README.md](assets/fonts/README.md) 里那张排除清单。

## 最新深链（2026-09-18）

新增 URL hash 深链：`index.html#char=休&mod=origin&view=writing` 可在页面加载时直达指定模块、选字与子页（`view` 白名单见 [src/ui/app-shell.js](src/ui/app-shell.js) 的 `DEEP_VIEWS`，quiz 额外支持 `level=mission|classic`，与原有 `#q=` 题种子 `&` 共存）。深链一次性消费：跳转后 `view`/`level` 即失效，手动切模块回到各模块默认子页；只在加载时解析，不监听 hashchange。已补 `verify/test-delivery.mjs` 回归（合法/非法参数、白名单过滤、一次性清空、四组件子页初值、与 `#q=` 共存）。七模块基础状态不变。

## 最新成品补强（2026-09-18）

**06 汉字关系网落地。** 这是本轮唯一的新模块，也是 128 字扩容的最后一块拼图 ——
在此之前，28 个关联字除了 catalog 里的一行以外，在界面上一个入口都没有。

它画的是**以当前字为中心的一到两层邻域**，不是 128 个节点的总览（两跳最坏是「木」的
22 个节点，带标签在 560×560 里画得下；128 个画不下）。关系来自两处，都是已经写在数据里的，
不新造事实：

- **构形关系（实线）** 取自构件表 `Components.DECOMPOSITION`；
- **相关字（虚线）** 取自教学目录的 `related` 一列；
- 同一对字两处都命中时**合并成一条**，按构形算。这不是优化是必需：25 条构件边里
  23 条与 `related` 重合（只有「安—宀」「李—子」是构件独有的），不合并就是同一对字叠
  两条线，而且偏偏叠在木（13 度）、人（6 度）、子/身/上/马（各 5 度）这些**度数最高的
  枢纽**周围 —— 也就是每一张邻域图的中心区。

「实线是构形事实、虚线是语义相关」这条分工写在 `data/catalog.js` 文件头，由代码机械
判定，不靠读的人猜。第 7 条关系类型是**人工推荐探索路径**（`data/paths.js`，四条：
木→林→森、人→从→众、人+木→休、日+月→明）。路径**不携带任何新关系**，只是一串跳转，
每个跳转都必须在派生边里找得到 —— 找不到就报错。这条由 `verify/test-graph.mjs` 钉着，
所以不存在「两份真相」。

交互：点图上任意一个字就换成以它为中心；「返回上一个字」和 `Esc` 退回上一级（一个栈，
不设这个走进三层就回不去了）；左列画布下面是路径选择器和**读图图例**（实线/虚线各是什么，
各配一段与画布同线型的色块；末尾一行写「本字这一圈多少字、全站多少条连线」），
右栏只留中心字的释义构形与按两类关系分组的邻居。
关联字**可以当图的中心，但不能当全站选字**（它们没有楷书字形，切到 01/02/03 会空转）——
这两件事在 `graph-view.js` 里是两个变量，点关联字时图会停在那儿、同时带你到它最近的一个
有字形的字。`宀` 是唯一一个不在 catalog 里的构件（安 的部件），它作为有标签的节点保留，
不静默丢掉。

**数据补空。** 17 个核心象形字的 `related` 整行是空的（这也让它们在 01 里连「关联汉字」
那一行都没有）。本轮按「同类 + 字源」补齐，绝大多数不是新造关系，而是把已经存在的单向边
补成双向。补完后**全站不再有孤立字**，连通分量从 9 个降到 4 个：

| 大小 | 成员 |
| --- | --- |
| 114 | 主分量 |
| 8 | 一 二 三 五 十 百 千 万 —— 数词自成一体 |
| 4 | 鼎 斗 勺 壶 —— 器物，没有站得住的关系可用 |
| 3 | 日 月 明 —— 内部是闭环三角，但和主分量没有可用的边 |

后三簇如实保留，**不为了「连成一片」硬凑关系**。这也是关系网侧栏那张统计行要
如实写出来的东西：它不粉饰图的连通度。

**背景**：第 7 张 Met 公版图（青花云龙纹罐，明宣德，馆藏 37.191.1，3000×4000，1.4 MB）。
这一页画的是字与字的连线，云龙纹本身就是一圈圈互相勾连的线条；蓝白釉色也与其他六张
水墨、拓本、设色拉得开。**如实说明：这是器物照片，其余六张都是纸绢绘画或拓本。**

**验证**：`node verify/run-all.mjs` **34 项全部通过**（Node 21 + 浏览器 8 + 静态自检 5），
比上一轮多 2 项 —— 新增关系网数据层（纯 Node）和关系网 UI（CDP）。
上一轮遗留的「浏览器用例仍有五模块断言、请勿引用旧报告」这条**已经作废**：
模块数、页签编号、背景张数、键盘用例借用的空地、七模块冒烟名单都更新成了当前值。
仍然**没有**做的：浏览器里的**人眼**视觉验收（连线压不压字、标签重不重叠、侧栏读不读得通），
以及新增 70 字的逐字目视核对 —— 自动断言撑满不等于内容正确。

**版面修正（同日，按用户反馈）。** 初版把四个面板全堆在右栏，实测**右栏 2215px、左栏只有
637px** —— 画布（正方形 544px）下方空出约 1500px 的死区，就是反馈里的「左侧太空」；
而底栏是 `position:fixed`，这一页的文档高又有 2437（超屏 1693），钉住的底栏于是浮在正文
中间、横切过侧栏（截图里它正压在「推荐探索路径」上）。两处都改了：

- **分栏改成按面板高度分**，而不是按「画布 / 其他」分：左列 = 画布 + 控件 + 推荐探索路径
  + 读图图例，右栏 = 中心字 + 本字的关系。前两个面板讲的都是「这张图」，本来就该
  挨着图放；右栏留下的两块讲的都是「这个字」。两栏高度差从 **1578px 降到 99–113px**。
- **底栏与页脚改回正常页面流**（`position:static`），随滚轮走。**只改 06**：02 和 05 的
  文档高（751 / 744）根本够不着视口，那条 `fixed` 在它们身上只是把底栏钉在屏幕底部，
  改成流式反而会在下面空出一截；06 才是唯一一个 `fixed` 真正在起作用、而且是帮倒忙的
  页面。01 / 03 / 04 本来就走这条路，06 只是加了进去。
- **顺带去掉 `--canvas-edge` 里的 `calc(100dvh - 300px)`**。那个约束是为了让画布一屏放得下、
  整页不用滚 —— 06 本来就要滚，留着它只会让画布在矮屏上白白缩水（1440×768 上会缩到
  400px）。现在 1050px 宽以上恒为 545–560px。
- 模块标题从右栏顶部提到整幅第一行、横跨两栏：窄屏回落成单列时它仍在画布上方，
  不像原先放在右栏那样会跟到画布后面，变成「看完图才看见页名」。
- **「这张图的实话」面板改成读图图例**（同日，按用户反馈）。原先那段统计有四个毛病：
  标题像在替别处担保；「128 字，其中 128 字有至少一条关系」是同义反复；「互不相连的簇」
  是内部说法；「没有连上的字…不是画不出来」在替一个**空集**辩护（全站早已没有孤立字）。
  它解决的是作者的顾虑，不是读者的问题 —— 读者第一次看见两色线，要的是「实线和虚线
  分别是什么」。现在面板写这两条，各配一段与画布**同线型的色块**（实线 2px / 虚线
  `dashed`，取自 `.zc-graph-edge` 那两条规则，不另造一套），末尾一句统计。
  举例是核对过边类型的：**「木—林」是实线**（林由木构成，属 `DECOMPOSITION`），
  能当虚线例子的是「水—川」（`related`）—— 按印象写就会错。
  `src/ui/app-shell.js` 的「使用帮助」里那句同源辩护话一并换掉了（它还指着「侧栏的统计」，
  而那块已经不在了，属于会被指出来是假话的那种陈旧引用）。

1050×600 至 2560×1440 七档实测：始终两栏、底栏不压内容、文档高恒为 1667（不再随视口变）。
`verify/test-gallery.mjs` 新增正反两条断言钉住 06 的画布必须保持 `relative` —— 它和
01/03/04 那条流式规则的选择器**特异性打平**（都是 0,3,0）而 gallery.css 在后，
一旦把 06 混进去，`relative` 会被静默覆盖成 `static`，空态那句「此字暂无关联」就跑到
画布外面去，而原来的断言照样是绿的。这条新断言已用变异测过：另写一条
`.is-workbench:is(.zc-page-graph) .zc-canvas-wrap { position:static }` 时它会红。

## 最新成品补强（2026-09-17）

最新 01 / 03 布局补强：针对截图中的画布下方大段空白，把两列拆成独立展台与右侧展签。01 的现代画布、书写工具（仅书写页显示）、小展签与唯一选字 / 搜索入口在左列自然叠放，字义及出处仍在右列；书写页也保留右侧字义说明。03 两种学习模式使用同一个画布底部插槽，将操作提示与已有「带入创作」入口放在画布下方，右列保持构件列表 / 验证工具，转交状态仍由原父层控制，不创建第二套引擎。画布尺寸、正常页面滚动、龙藏标题和背景均不变；新增入口去重、左列内部顺序、插槽边界与模板编译回归，18 项纯 Node 检查通过，浏览器视觉仍待用户查看。

最新滚动与尺寸调整：按用户最新要求，01 汉字探索 / 03 构形实验室 / 04 汉字挑战的主画布和底部状态栏改为正常页面流，随整页滚轮上下移动，不再 fixed 或 sticky；对应许可页脚也恢复正常流，取消固定栏的额外底部留空。02 千年演变 / 05 我的造字保留原固定方式。01 / 02 / 03 共用相同桌面画布尺寸公式（最大 560px，沿用演变工具带预算），小屏统一单列、同宽正方形和内边距。不改龙藏标题、其他字体与背景；新增对应静态回归检查，实机视觉未验收。

最新品牌标题：用户选择 6 号龙藏体，仅左上角「字启千年」切换为真实 Long Cang Regular，本地文件及 OFL 1.1 许可随项目保存。桌面工作台标题放大至 48px（原 40px），首页 42–52px、小屏 40px；保留字体原生笔触，不伪造粗体。正文 / 按钮及其他模块标题仍使用上一轮恢复的 Noto 系列；背景、布局和画布固定方式不变。

最新字体回退：按用户要求取消本轮霞鹜文楷，恢复改版前的 Noto Sans SC 正文 / 按钮与 Noto Serif SC 标题；保留已选展廊布局、背景光场、字号、文字颜色及画布滚动方式，未删除字体资源。更新字体许可说明与回归断言。

最新选定 UI：采用方案 A 的浅色开放展廊氛围、左侧画布 / 右侧展签布局，使用霞鹜文楷 Regular 接近方案 B 的书写感字体，不声称与生成效果图逐像素一致。取消说明侧栏纸色底板与多数大块卡片，统一说明字号和深色正文，宽屏品牌 / 模块索引合成一行；保留六张不同馆藏图与图像层原浓度，改用全屏连续光场隔开背景字纹。演变滑块 / 真实阶段入口 / 播放工具移到画布下方，阶段入口可键盘操作。用户已明确确认「画布留在可见区域，只有说明和工具上下滚动」；沿用桌面主画布可见区域固定、不随滚轮移动的方式，小屏自然排版。新增方案样式、字体离线许可、标签去重与展台尺寸预算检查；17 项纯 Node 检查通过，最终浏览器视觉与鼠标操作未验收。

最新古文字资料补强：新增「出处与实证」独立阅览，汉字探索字义页与千年演变工具侧可打开；支持换字/分类、并排对照、缺失/加载失败提示、外部出处/许可链接和引用记录选中复制，打开时暂停背景书写或演变。公开查询采集日月木山人休明好安共 53 张小学堂 CC0 查询图、54 份查询结果与许可留档，9 字有汉代隶变例；休秦系简牍未选得。原图为数据库整理字形图，不是原拓；不强行放大低分辨率图填主时间轴，不改变旧 CDP 授权和覆盖统计。新增图像 CRC、可见墨迹、哈希、字头/著录一致性和阅览边界检查；当前 16 项纯 Node 检查通过，浏览器及专业释读未验收。完整记录和人工清单见「参赛准备/古文字出处与许可核查.md」。

最新背景分配：首页与五个模块改为六张不同的本地馆藏原图，加入篆书碑拓和两种书法册页，其他为秋景、柳岸亭榭和竹石。图片逐张查看，核对官方 Public Domain / API 标记，完整来源见 `assets/backgrounds/README.md`。不改变用户确认的不透明度、背景全屏扩展、主画布固定与页面滚动设置。背景测试新增六图片内容哈希去重、JPEG 完整性、实际尺寸、全屏 cover 理论覆盖与浓度保持；当前 15 项纯 Node 检查通过，仍需浏览器确认最终取景与文字对比度。

最新模块分工：实验室取消「自由构字」和完整自由编辑器，只保留「认识构形 / 目标拼合」。新增「用这些构件去创作」，将当前例字的构件转交「我的造字」；先恢复原草稿，再弹出确认，确认后追加并解锁对应构件，已有位置、名称、含义及正式字库不自动覆盖。独体字带入一个构件，森等重复构件保留数量。追加超过 8 个时阻止操作，可取消后整理画布再从实验室带入；整组追加可一步撤销。未确认或取消不修改画布或解锁。转交是本次导航请求，不是永久保存的创作记录；成功带入后沿用草稿保存。新增该流程回归后，15 项纯 Node 检查通过；更新了浏览器实验室测试的二模式预期，但本轮没有执行浏览器测试。

最新挑战区分：两套独立题池。轻松探索每轮两题「古今相认」与两题「字义入境」，均为单步单选，有直接操作引导，不含校勘、证据双选、排序或拖拽。推理挑战每轮校勘、双字取证、时间排序、实际拼合各一；拼合先四选二筛选构件，再拖拽恢复布局，筛选错误可改但按辅助计分。林、森、从、众从所有挑战目标池移除，拼合只用休、明、好、安；探索与实验室的教学示例保留。取证改为六组不同构件对比，校勘采用更细微的方向/位置/比例差异。笔记重练遵从所属模式，旧题型迁移，不把难题混回轻松模式。当前 14 项纯 Node 检查通过，新增检查扫 2000 双模式轮次、200 组件整轮及 240 构件确认组合；这不是实机视觉/鼠标验收。

本轮在不依赖人工资料、不改变已确认背景透明度的前提下，新增：顶部「使用帮助」、六模块操作指南、可保存的大字阅读、运行接口与资料自检、本机练习笔记与六类同类重练、透明背景的当前创作 SVG 导出、创作方向键微调（10 单位，Shift 为 30 单位）。练习笔记最多 120 条（2026-09-17 从 30 条上调，字表扩到 128 字后 30 条会静默丢数据），记录答错和跳过、解析及同题后续答对状态；同类重练可能更换字，不代表原题一定被巩固。笔记、收藏、阅读设置、编辑草稿均不在 MINE1 字库备份中。SVG 是当前创作字形，不是海报，不代表真实新汉字。

稳定性修复：演变动画跳转先限制到合法阶段；无效时间值不污染引擎；并览点选单期时退出并览/对比；手动书写操作取消尚未启动的延迟自动播放；异常、超长及含继承属性的备份先校验清洗再合并，失败不修改原状态。此前拼字列表的响应与构件索引问题也已修复。

`node verify/run-all.mjs --fast` 当前 19 项纯 Node 检查全部通过，包含新功能、资料盘点、取消延迟播放及导入边界。它们不是浏览器视觉、真实鼠标操作、下载或用户研究验收。

浏览器自动化后来恢复了：`node verify/run-all.mjs`（不带 `--fast`）会另跑 6 项 CDP 驱动的
真实页面用例 + 5 项静态自检页，共 **30 项，约 2 分钟**。写本节时它不可用，当时的实测表
因此留空；那些空格没有事后追填 —— 要作数就重跑一遍。

实际资料盘点与许可缺口见「参赛准备/古文字资料盘点.md」；奖项自评与本轮取舍见「参赛准备/本轮成品补强与奖项自评.md」。下方早期轮次说明若与本节冲突，以本节为准。

最新挑战升级：默认「推理挑战」，可切换「轻松探索」，切换开启新一轮但保留解锁。推理轮仍为四任务：字形侦探或构形校勘二选一、双字取证、时间档案、拼合行动。字形侦探优先采用关联/近形候选；校勘提供同构件的四份 SVG 草稿，比较方向、位置与比例；取证从八组现代字形对比中选择两条正确证据，选齐再提交；时间排序和实际拖拽继续保留。可主动查看逐条线索，无倒计时；答对 100 分、辅助后答对 60 分、错题/跳过 0 分，答对数与构件解锁仍独立记录。拼字底纹提示计入辅助，复盘含所选草稿的具体差异、线索使用及得分。修正了挑战归位列表的响应依赖与原构件索引。12 项纯 Node 检查通过，新检查覆盖 1000 推理轮、证据组合枚举及 100 组件整轮；仍需浏览器实际操作和可用性验证。未改变背景不透明度。

最新覆盖此前背景浓度说明：按用户要求恢复上一次不透明度，首页 22%，五模块 44%/40%/42%/44%/42%，小屏首页 16%、工作台 28%。覆盖层参数同步恢复，移除上次新增的导航和首页文案浅色保护；保留饱和度、对比度和全屏裁边。滚动与主画布定位不变。

背景铺满修订：根节点显式清除 margin/padding，图片固定层向四边扩展 4vmax，配合 cover 裁掉原图浅色纸边；首页取消左右泛白遮罩。背景与正文最大宽度无关，不采用 100vw 放大正文，不改变画布定位和滚动逻辑。补充手机、平板、桌面及超宽屏的 cover 理论覆盖检查，仍需浏览器实际视觉确认。

最新背景设计：首页和 01—05 模块均使用大都会艺术博物馆开放图像 Landscape with Autumn Foliage（传沈周，馆藏 69.131.9），共用本地 JPEG 1950×1275。本次首页图片浓度提高至 62%，五模块为 70%—74%，恢复饱和度并微调对比，浅色覆盖降低至 8%—12%；小屏首页为 48%、工作台为 55%。标题导航与首页文案仅作局部浅色阅读保护，不模糊底图、不整体透明正文或画布。原图分辨率未提升，高分屏放大仍有细节上限。背景固定且 pointer-events:none，运行时无需联网。首页保留来源链接，出处与 CC0 政策记录见 assets/backgrounds/README.md。图片已独立查看，11 项纯 Node 检查通过，未完成实际浏览器整页视觉验收。

最新参赛补强：首页不编号，其余模块改为 01—05；顶部改为展览索引，底部改为展签式状态栏，显示真实探索字数、构件及作品数，存储失败时提示会话模式和备份。挑战新增当前轮次逐题复盘、回到对应字学习及前往创作；创作增加可撤销横向/纵向/环形布局草案，多构件自动编排分行以保留备份缩放一致性。模块栏可用方向键及 Home/End 切换，模态选字面板打开时暂停底层画布快捷键。参赛评估、申报文字与三分钟演示/实测表在「参赛准备」目录，均是草稿而非完成申报或获奖保证。

本轮继续完善：界面改为「纸白 × 墨绿」现代展览风格；首页五入口改为图标卡片，统一字体、编号、按钮与留白；新增推荐选字与动画暂停。自由构字新增选中框、撤销/重做（最多 50 个历史状态），造字草稿自动保存与恢复；海报增加「纸白展签 / 深墨夜色」两种风格，长含义自动换行，修改构件、含义、名称或风格后需重新生成海报。

最新首页调整：取消与顶部导航重复的五入口卡片，改为「木、日、人」可切换字形故事、四期代表字形选览与「木—林—森」构形展示。故事说明附教育部《异体字字典》参考链接，保留缺阶段、代表字形及古文字素材授权待确认的说明。首页仅保留开始探索和一处与当前故事相关的演变入口。

最新桌面布局：取消整个模块展示区的固定定位，内容区恢复自然文档流，可用鼠标滚轮上下浏览工具与说明，底部留出 100px 空间避免状态栏遮挡。桌面视口（宽度至少 1050、高度至少 600）仅独立固定探索、演变及对比、实验室与创作主画布，挑战题卡及其小拼字台自然滚动，无独立侧栏滚动容器。探索页为「大画布＋字义/字源/书写页签」，演变页为「字形或对比＋阶段/操作/选字页签」。实验室、造字区通过互斥操作页签减少堆叠，笔画列表每页 6 笔，作品字库每页 4 件；搜索选字用独立对话面板。标题四字统一字重；文化说明用较轻的 Noto Serif SC，按钮用轻字重 Noto Sans SC，取消大块构形标签。原 520 SVG 坐标系与 1000 × 1400 PNG 导出不变。小屏/低高度视口保留流式降级，不通过隐藏溢出来剪掉功能。当前已做模板、状态和 12 组桌面画布理论宽高检查，仍需实机浏览器确认滚动、固定区域、对话面板及字体的视觉效果。

新增功能：收藏汉字（本机持久化，存储失败则仅本次会话）、选字面板的收藏筛选、首页 3 分钟体验路线，以及字义页的读音按钮。朗读仅选择 localService 为 true 的中文系统语音；浏览器不支持、语音未安装或加载未就绪时显示拼音降级提示，不调用远程语音。收藏为独立本机偏好，暂不包含在 MINE1 造字备份码中。字库备份改为单独页签，不再向下展开。

自动书写残影修复：SVG 圆形虚线端点在进度为零时仍可能露出墨点，因此引擎现在在创建和每次更新时明确隐藏进度为零的画笔；点画也不在零进度出现。探索书写和挑战成功奖励取消淡色整字底纹，探索不再在自动播放前先显示完整汉字。`show/clear` 同时更新引擎进度快照。纯 Node SVG 状态回归覆盖 30 字的创建、前进、倒退、重播、清空、部首筛选及自动播放前状态；这不是浏览器像素验收。

## 打开方式

在比赛电脑上，用 Chrome 或 Edge 打开本文件夹内的 `index.html`。请保留整个文件夹，不能只复制 HTML。
运行依赖、字形和代码均已放在本地，页面没有运行时在线请求，无需注册、安装 npm 或启动后端。
本轮保留现有的 Vue 3 经典脚本架构，没有迁移到 Vite；这是本地离线演示版本，不是已部署的网站。

## 已实现的部分

| 模块 | 当前实现 |
| --- | --- |
| 首页 | 作品介绍、动态代表字形、开始探索、推荐选字、三个字形故事、构形展示、探索进度；统一顶部导航 |
| 汉字探索 | 100 字卡片；汉字、拼音、字义搜索；分类；拼音、字义、构形与文化说明；关联汉字；现代笔画动画 |
| 千年演变 | SVG 代表字形、拖动时间轴、连续淡入淡出、播放/暂停、阶段信息、并览、可选择两期的对比模式 |
| 构形实验室 | 15 字拆解与构件拖动；目标位置吸附拼合、提示、成功反馈；把当前构件转交我的造字，不再提供重复自由编辑器 |
| 汉字挑战 | 独立两题池：轻松为古今相认/字义入境单选；推理为校勘/双字取证/排序/筛选构件后拖拽；辅助计分、正误解析、连答、解锁、复盘和笔记 |
| 我的造字 | 26 个可解锁构件，初始开放 6 个；自由摆放、缩放、旋转、位置微调、删除；输入含义与创作名；本机作品保存、继续编辑、备份与恢复 |
| 数字海报 | Canvas 绘制实际笔画轮廓和布局，保留旋转；作品名称、构件、含义、创作声明；1000 × 1400 PNG 导出 |

可拆解和拼合的示例：休、明、林、森、从、众、好、安、鸣、相、炎、岩、男、坐、李。
其中 14 个会意字，李 是唯一的形声字（「木」意符 + 「子」声旁，声旁不表意）。
15 字之外的字提供探索与独体字形体验，不是每个字都能继续拆分。
汉字构形判断只匹配这 15 个示例，不能作为识字系统，也不会证明自由组合是新发现的真实汉字。

进度口径：同一个字访问至少两个有效学习模块后计为「已探索」，保存于本机浏览器。进入挑战页不再计当前选字的学习；挑战仅在实际答对时给该题目标字记录 quiz 模块。
作品最多保存 60 件，每件最多 8 个构件。「我的造字」编辑稿在操作后约 300 毫秒自动保存为本机草稿，切换模块时立即保存，再次进入会恢复；恢复的是构件、含义、名称及海报风格，不包含历史撤销记录或旧海报图片。正式作品仍建议保存到字库并复制备份码，草稿不会包含在作品备份码中。
本机存储失败时会改为会话内存，并提供备份码；请及时备份。更换浏览器、设备或文件位置可能无法看到原来的记录。

## 尚未达到原需求的部分

1. **古文字素材还需审核。** 目前是代表字形素材集，未建立逐字、逐期的器物编号、拓片出处和释读依据，不能宣称完成了完整可靠的字源数据库。
2. **没有隶书素材。** 当前阶段为甲骨文、金文、楚系简帛、战国金文、小篆及现代楷书。不能将战国字形冒充隶书。楚系简帛与战国金文属于同一大时期，演变排序题不强制两者的先后。
3. **不是路径 Morph。** 演变动画是连续 SVG 图层过渡，不是逐笔画变形。可靠的路径变形需要选定对应字形、建立笔画配对或手工关键帧；有合适素材后可继续由我实现，不需要你自己编动画。
4. **首批没有形声字教学样例。** 目前围绕象形与组合示例完成互动，没有为了凑齐类别临时增加未经核对的样本。
5. **以模块切换代替独立路由。** 没有实现需求建议中的八个独立 URL、Vite 构建、后台或线上部署；作品生成在「我的造字」内完成。拆字与拼字位于实验室，猜字在挑战模块中，不是三个独立游戏页面。
6. 音效、成就和更多关卡未实现。海报目前有两种配色模板，尚无更多版式。这些是后续增强，不是当前流程的前提。

## 需要你完成或提供的事项

### 1. 核对参赛素材与内容

请与指导老师或汉字研究相关人员确认：**128 条**教学目录（其中 100 字带古文字字形）的古文字对应、释读、简繁对应、造字法和文化说明是否可用于你的比赛。
尤其「明、好、安」的现代构件教学不等于全部历史字源；不要只根据今天的拆字推断古代含义。
建议查核[教育部异体字字典](https://dict.variants.moe.edu.tw/)与[中央研究院小學堂](https://xiaoxue.iis.sinica.edu.tw/Home/Home)。

如果需要原需求规定的五期演变，请提供或指定**授权明确、释读可靠的隶书及缺失字形**，并附字、时期、来源链接/编号及许可。
拿到经过确认的素材后，我可以继续整理 SVG、补阶段、修订说明和完善动画。单纯换一种现代字体不算可靠古文字资料。

### 2. 确认本地古文字文件的授权范围

项目内古文字部分提取自「中央研究院漢字構形資料庫 2.4 版」。
官方[釋出聲明](https://cdp.sinica.edu.tw/cdphanzi/declare.htm)确实为字体列出 GFDL 1.2 / CC BY-SA 2.5 TW 双授权，但其版本说明较本地 2.4 版新。
目前不能直接断言该声明覆盖本地这批文件及提取后的 SVG，页面保留了授权待确认标记。

请向发布方确认版本与衍生字形的许可范围，或改用授权明确的版本重新提取。
官方页面提供联系邮箱 `cdpservice@iis.sinica.edu.tw`；本轮没有代你发信。
询问时可说明：使用 **100 字**的代表轮廓（共 484 个字形，五期分别 95/97/94/99/99）、转换为 SVG、用于学生互动 Web 作品、可能离线参赛与公开展示，请对方确认版本范围及应保留的署名/许可。
⚠️ 这里**不要按 30 字报** —— 那是扩容前的规模。128 字已是分发形态的改变，按小规模询问
拿到的答复覆盖不了实际用量，等于没问。
正式公开展示或分发前解决这项问题；详情见 `assets/vendor/LICENSES.md`。这不是法律意见或使用许可。

### 3. 在你的比赛设备完成实测

本轮工具在打开本地 `file://` 页面时被安全策略拒绝，因此没有完成最终浏览器视觉与鼠标拖拽验收，也没有验证真实 PNG 下载文件。
这不意味着你的浏览器不能打开；请你在实际设备上检查下列项目，并把截图或错误信息发给我继续修复。

- 断网后打开 `index.html`，七个模块都能进入，字形无缺失或乱码。
- 搜索「休」及「xiu」，关联字选择和各模块共用选字正常。
- 「休」拆解、重组、拖回目标并自动吸附；提示和成功动画正常。
- 时间轴拖动、播放、并览与两期对比正常；缺素材时有说明。
- 完成一轮四题，确认拖拽构形、排序、得分与新构件解锁。
- 用「日 + 心」创作，缩放、旋转、输入「快乐」，生成并下载 PNG；打开图片检查实际轮廓、布局和文字。
- 保存后刷新，能继续编辑；备份码可恢复作品与旋转角度。
- 不保存字库直接切换模块再返回，或稍等后刷新，草稿能恢复；撤销、重做和清空后的撤销正常。草稿恢复后再次生成海报，检查两种配色及长含义换行。
- 在投影或比赛分辨率上检查字号、滚动及按钮；建议测试 1366 × 768 和 1920 × 1080。

参赛者署名、作品介绍、原创性声明与 AI 辅助使用披露须按真实开发过程和比赛规定填写，不能声明全部代码或外部字形均由自己手写原创。
若还需要公开网站，请先确定发布权限、平台或域名；本轮未上传项目，也未购买服务。

## 已做的程序验证

`node verify/run-all.mjs` 现在跑 **34 项**：21 项纯 Node 逻辑检查 + 8 项 CDP 驱动的
浏览器用例 + 5 项在页内自己判定的自检页。`--fast` 只跑其中的 Node 层 21 项。
**这些都不等于浏览器视觉验收** —— CDP 能证明「元素挂上了、像素级坐标在画布内、
监听器归零」，证明不了「连线压不压字、标签重不重叠、侧栏读不读得通」。

```text
node verify/run-all.mjs          # 34 项，含浏览器层
node verify/run-all.mjs --fast   # 只跑 Node 层 20 项
node verify/test-delivery.mjs
node verify/test-mine.mjs
node verify/test-compose.mjs
node verify/test-quiz.mjs
node verify/test-geometry.mjs
node verify/test-stroke-visibility.mjs
node verify/test-experience.mjs
```

新增交付检查覆盖 34 个脚本加载、15 个 Vue 模板编译、教学目录完整性、七模块导航、构件锁定、缩放中心不变、旋转保存/备份/模拟重开、Canvas 笔画绘制、存储失败降级、挑战得分与解锁，以及 400 个种子的界面演变排序约束。
继续完善的检查增加：首页五入口编号与图标、暂停播放、撤销重做/分支/历史上限、草稿恢复及存储失败降级、海报失效处理和长文本换行。UI 已完成代码层调整，仍未完成浏览器截图级视觉验收。
最新检查用首页三个故事的选字、字形覆盖和上下文导航替换已删除的五入口断言。

**字库已经扩充**：`data/catalog.js` 128 条（36 核心 + 64 扩展 + 28 关联），
`data/chars.js` 100 字，`data/ancient.js` 覆盖其中 100 字（五期分别 95/97/94/99/99，
91 字五期齐全）。可拆解字从 8 涨到 15，构件盘 24 → 26。

**28 个关联字现在看得到了**：06 汉字关系网已落地（见本节顶部的 2026-09-18 条），
它们仍然没有楷书字形与古文字 —— 那是数据缺口，不是入口缺口 —— 但可以在关系网里
被点开、当前提。**图鉴视图（128 格可筛选网格）仍未开工**，那是关联字被浏览的另一条路。

旧 `verify/run-all.mjs` 中那些“五模块 / 旧标签”的断言**已于 2026-09-18 全部更新**：
模块数、页签编号、背景张数、键盘用例的空地选择、六模块冒烟名单都改成了当前值，
整套用例重新跑绿（见下方 2026-09-18 条）。本行原文是「本轮没有更新，请勿引用」，
那句话现在不成立了，留在这里是为了说明它曾经是什么状态。

## 三分钟演示建议

0:00–0:30 首页开始探索「休」，看拼音、字义与人/木构形。

0:30–1:00 看演变时间轴，开启甲骨文与楷书对比。

1:00–1:40 进入实验室，拆解「休」，再完成一次吸附拼字。

1:40–2:10 体验挑战题与构件解锁；按实际演练速度决定是否做完整四题。

2:10–3:00 用日、心自由创作，赋义「快乐」，生成海报并保存 PNG。

正式演示前请先确认素材许可与内容，不要把原型状态包装成已完成学术审核的正式成品。
